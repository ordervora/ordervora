import { CustomersManager } from '@/components/dashboard/CustomersManager';

/** Customers — profiles, loyalty, history, addresses, VIP. */
export default function CustomersPage() {
  return <CustomersManager />;
}
