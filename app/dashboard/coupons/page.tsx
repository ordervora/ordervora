import { CouponsManager } from '@/components/dashboard/CouponsManager';

/** Coupons — create, edit, usage analytics, expiry, limits. */
export default function CouponsPage() {
  return <CouponsManager />;
}
