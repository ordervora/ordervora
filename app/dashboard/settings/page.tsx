import { SettingsManager } from '@/components/dashboard/SettingsManager';

/** Settings — branding, hours, fulfillment, taxes, loyalty, Stripe, sound. */
export default function SettingsPage() {
  return <SettingsManager />;
}
