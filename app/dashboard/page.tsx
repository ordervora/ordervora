import { DashboardOverview } from '@/components/dashboard/DashboardOverview';

/** Dashboard home — KPIs, live orders, best sellers, sales chart, activity. */
export default function DashboardHomePage() {
  return <DashboardOverview />;
}
