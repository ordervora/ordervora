import { MenuManager } from '@/components/dashboard/MenuManager';

/** Menu — categories, products, availability/86, modifiers, inventory. */
export default function MenuPage() {
  return <MenuManager />;
}
